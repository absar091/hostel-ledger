## 2024-05-24 - Replace native title attributes with custom Tooltips
**Learning:** Native `title` attributes on icon-only buttons provide inconsistent styling and delay. Replacing them with custom, accessible Radix Tooltips improves the user interface consistency while maintaining accessibility via `aria-label`.
**Action:** Use `<Tooltip>` and `aria-label` instead of `title` for better UX.
