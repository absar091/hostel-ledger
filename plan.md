1. **Add Tooltip Imports:** Use `replace_with_git_merge_diff` on `src/components/TransactionDetailModal.tsx` to add `Tooltip`, `TooltipContent`, `TooltipProvider`, and `TooltipTrigger` imports from `@/components/ui/tooltip`. Verify with `git diff HEAD src/components/TransactionDetailModal.tsx`.
2. **Add Opening TooltipProvider:** Use `replace_with_git_merge_diff` to add the opening `<TooltipProvider>` tag just after the empty fragment `<>` in the return statement of `TransactionDetailModal.tsx`. Verify with `git diff HEAD src/components/TransactionDetailModal.tsx`.
3. **Add Closing TooltipProvider:** Use `replace_with_git_merge_diff` to add the closing `</TooltipProvider>` tag just before the closing empty fragment `</>` in the return statement of `TransactionDetailModal.tsx`. Verify with `git diff HEAD src/components/TransactionDetailModal.tsx`.
4. **Update Share Button:** Use `replace_with_git_merge_diff` on `src/components/TransactionDetailModal.tsx` to replace `title="Share as Image"` with a Radix `Tooltip` on the share button and add an `aria-label="Share as Image"`. Verify with `git diff HEAD src/components/TransactionDetailModal.tsx`.
5. **Update Close Button:** Use `replace_with_git_merge_diff` on `src/components/TransactionDetailModal.tsx` to add an `aria-label="Close transaction details"` to the Close button. Verify with `git diff HEAD src/components/TransactionDetailModal.tsx`.
6. **Update Copy Button:** Use `replace_with_git_merge_diff` on `src/components/TransactionDetailModal.tsx` to replace `title={isCopied ? "Copied!" : "Copy Reference"}` with a Radix `Tooltip` that says "Copied!" or "Copy Reference". Include an `aria-label={isCopied ? "Copied!" : "Copy Reference"}` attribute on the button. Verify with `git diff HEAD src/components/TransactionDetailModal.tsx`.
7. **Update Test File Selector:** Use `replace_with_git_merge_diff` to modify `src/components/__tests__/verify_copy_feedback.spec.ts` from `page.locator("button[title='Copy ID']")` to `page.locator("button[aria-label='Copy Reference']")` and from `page.locator("button[title='Copied!']")` to `page.locator("button[aria-label='Copied!']")` in a single contiguous `SEARCH/REPLACE` block. Verify with `git diff HEAD src/components/__tests__/verify_copy_feedback.spec.ts`.
8. **Test:** Use `run_in_bash_session` to run `pnpm lint && pnpm test src/components/__tests__/verify_copy_feedback.spec.ts && pnpm run build --mode development` to ensure changes don't break anything.
9. Complete pre-commit steps to ensure proper testing, verification, review, and reflection are done.
10. **Submit PR:** Use `submit` to create the PR, with title format `🎨 Palette: [UX improvement]` and description containing `💡 What`, `🎯 Why`, `📸 Before/After`, and `♿ Accessibility`.
